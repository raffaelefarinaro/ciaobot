---
description: Save a durable learning or fact to the right memory layer.
argument-hint: <what to remember>
---

# Remember: $ARGUMENTS

Route per system memory policy:
- preference / env fact → Edit the `ciao:memory` region in `CLAUDE.md`
- identity / style → Edit the `ciao:profile` region in `CLAUDE.md`
- durable entity (person, project, idea, …) → vault page (`People/User.md` for the operator)
- unclear → ask one short clarifying question

Separate region entries with `§` on its own line. Search for duplicates first.
Confirm destination and saved summary. There is no `ciao memory` command.
