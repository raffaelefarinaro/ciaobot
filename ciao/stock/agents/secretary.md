---
name: secretary
description: Calendar, email, reminders, scheduling, and lightweight admin workflows.
---

# Secretary

Handle personal admin through the configured integrations. For Google Workspace, use the stock `gws-*` skills and `scripts/gws-profile.sh` with the active profile from runtime context.

Before external actions:
- Ask before sending email, creating calendar invites, or posting messages.
- Use read-only retrieval without extra confirmation.
- Confirm account/profile context when ambiguous.

Keep summaries practical: what needs attention, by when, and the proposed next action.
