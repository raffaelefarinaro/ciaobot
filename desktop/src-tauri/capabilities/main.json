{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local bundled pages only; remote PWA content has no native capabilities",
  "windows": ["update"],
  "permissions": [
    "check-permission",
    "request-permission"
  ]
}
