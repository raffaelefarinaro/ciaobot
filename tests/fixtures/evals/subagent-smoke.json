{
  "schema_version": 1,
  "name": "Packaged subagent live smoke",
  "defaults": {
    "provider": "claude",
    "model": "sonnet",
    "surface": "mcp"
  },
  "scenarios": [
    {
      "name": "researcher-verifies-python-removeprefix",
      "description": "Delegates a stable self-contained check to the packaged researcher and waits for parent synthesis.",
      "target": {
        "kind": "subagent",
        "name": "researcher"
      },
      "prompt": "Ask the researcher to compute and briefly explain the result of 'TestHook'.removeprefix('Test'). This is a self-contained language check, so no current external lookup is needed. Wait for the researcher to finish, synthesize the finding, and finish with this exact final line: SUBAGENT_SMOKE_OK",
      "assertions": {
        "output_contains": [
          "Hook",
          "SUBAGENT_SMOKE_OK"
        ],
        "output_not_contains": [],
        "output_regex": [],
        "output_not_regex": [],
        "required_tools": [
          "agent"
        ],
        "forbidden_tools": []
      }
    }
  ]
}
