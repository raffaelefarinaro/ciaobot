{
  "schema_version": 1,
  "name": "Packaged skill live smoke",
  "defaults": {
    "provider": "claude",
    "model": "sonnet",
    "surface": "mcp"
  },
  "scenarios": [
    {
      "name": "ciao-capabilities-project-list",
      "description": "Loads the packaged capability catalog and exercises a read-only Ciaobot tool.",
      "target": {
        "kind": "skill",
        "name": "ciao-capabilities"
      },
      "prompt": "Use the capability catalog to answer this request: what is Ciaobot's hierarchy for chats, projects, and workspaces? Call the projects_list tool exactly once to inspect the isolated workspace. Finish with this exact final line: CIAOBOT_SKILL_SMOKE_OK",
      "assertions": {
        "output_contains": [
          "CIAOBOT_SKILL_SMOKE_OK"
        ],
        "output_not_contains": [],
        "output_regex": [
          "(?i)workspace\\s*→\\s*project\\s*→\\s*chat"
        ],
        "output_not_regex": [],
        "required_tools": [
          "projects_list"
        ],
        "forbidden_tools": []
      }
    }
  ]
}
